# Smoke test: verifies the app starts and shows the main menu.
# This is the fastest sanity check — run on every commit.

test "the app launches successfully"
  @smoke @critical
  open the app
  wait 3 seconds
  see "Lector"
  see "Dígita"
  don't see "Error"
  don't see "Crash"
