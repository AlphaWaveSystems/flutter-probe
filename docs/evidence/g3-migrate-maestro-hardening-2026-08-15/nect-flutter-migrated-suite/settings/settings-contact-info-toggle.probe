# Converted from Maestro — app: ${APP_ID}

test "migrated flow"
  use "../../helpers/login.yaml"
  use "../../helpers/open-drawer.yaml"
  tap on #drawer_settings_item
  wait until "ACCOUNT" appears
  scroll down
  see #settings_contact_info_toggle
  take a screenshot called "settings-contact-info-toggle-before"
  tap on #settings_contact_info_toggle
  wait for the page to load
  take a screenshot called "settings-contact-info-toggle-after"
  tap on #settings_contact_info_toggle
  take a screenshot called "settings-contact-info-toggle-restored"
