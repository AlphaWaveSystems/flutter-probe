# Converted from Maestro — app: ${APP_ID}

test "migrated flow"
  use "../../helpers/login.yaml"
  use "../../helpers/open-drawer.yaml"
  tap on #drawer_settings_item
  wait until "ACCOUNT" appears
  tap on #settings_change_password_tile
  don't see "Enter your email"
  take a screenshot called "settings-change-password-dialog"
  tap on "Cancel"
  wait until "ACCOUNT" appears
