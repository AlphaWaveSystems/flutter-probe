# Converted from Maestro — app: ${APP_ID}

test "migrated flow"
  use "../../helpers/login.yaml"
  use "../../helpers/open-drawer.yaml"
  tap on #drawer_settings_item
  wait until "ACCOUNT" appears
  scroll down
  see #push_notifications_toggle
  scroll down
  see #email_notifications_toggle
  take a screenshot called "settings-notifications"
