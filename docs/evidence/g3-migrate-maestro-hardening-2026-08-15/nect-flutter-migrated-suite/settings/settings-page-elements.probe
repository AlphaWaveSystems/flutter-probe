# Converted from Maestro — app: ${APP_ID}

test "migrated flow"
  use "../../helpers/login.yaml"
  use "../../helpers/open-drawer.yaml"
  tap on #drawer_settings_item
  wait until "ACCOUNT" appears
  see "ACCOUNT"
  see "NOTIFICATIONS"
  scroll down
  see "PRIVACY"
  scroll down
  see "APP PREFERENCES"
  scroll down
  see "ABOUT"
  take a screenshot called "settings-all-sections"
