# Converted from Maestro — app: ${APP_ID}

test "migrated flow"
  use "../../helpers/login.yaml"
  use "../../helpers/open-drawer.yaml"
  tap on #drawer_settings_item
  wait until "ACCOUNT" appears
  see #settings_edit_profile_tile
  tap on #settings_edit_profile_tile
  wait until "My Profile" appears
  see "PERSONAL DETAILS"
  take a screenshot called "settings-edit-profile-tile-navigated"
  tap on #profile_back_button
  wait until "ACCOUNT" appears
