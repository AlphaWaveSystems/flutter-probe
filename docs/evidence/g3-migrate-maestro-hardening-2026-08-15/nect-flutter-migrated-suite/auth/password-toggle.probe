# Converted from Maestro — app: ${APP_ID}

test "migrated flow"
  use "../../helpers/clear-state.yaml"
  wait until "Login to NECT" appears
  see "Show password"
  tap on "Show password"
  see "Hide password"
  take a screenshot called "password-visible"
  tap on "Hide password"
  see "Show password"
  take a screenshot called "password-hidden"
