# Build artifacts
bin/
coverage.out
*.exe
*.test
*.prof
# Local CLI binaries built into repo root (use -o to control output otherwise)
/probe
/probe-mcp

# Studio (Wails) build artifacts
studio/build/bin/
studio/frontend/node_modules/
studio/frontend/dist/
studio/frontend/.vite/

# Sensitive/private documents
LAUNCH_PLAN.md
MARKET_RESEARCH.md

# Cloud service (private, moves to separate repo)
/cloud/

# Go
vendor/

# Dart / Flutter
probe_agent/.dart_tool/
probe_agent/build/
probe_agent/pubspec.lock
probe_agent/.packages

# VS Code extension build output
vscode/node_modules/
vscode/out/
vscode/*.vsix
vscode/.vscode-test/

# Website build output
website/node_modules/
website/dist/
website/.astro/

# Reports (generated test output)
reports/

# OS files
.DS_Store
Thumbs.db

# IDE
.idea/
*.swp
*.swo
*~

# Environment
.env
.env.*
*.env

# Credentials / secrets
*.pem
*.key
*service-account*.json
credentials.json

# MCP config (contains credentials)
.mcp.json

# Claude Code local config
.claude/

# Local probe configs (project-specific, not framework defaults)
probe.yaml
probe.*.yaml

# Playwright MCP artifacts
.playwright-mcp/

# Firebase debug logs
firebase-debug.log

# Screenshots (local dev artifacts in repo root)
/*.png
CLAUDE.md
